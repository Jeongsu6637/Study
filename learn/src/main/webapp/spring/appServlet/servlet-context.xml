<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:mybatis="http://mybatis.org/schema/mybatis-spring"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:context="http://www.springframework.org/schema/context"
             xsi:schemaLocation="
        http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
        http://mybatis.org/schema/mybatis-spring
        http://mybatis.org/schema/mybatis-spring.xsd">

    <annotation-driven />

<!--     "com.learn" 패키지와 그 하위 패키지에서 컴포넌트를 스캔하고, Spring의 구성에 자동으로 등록하는 역할 -->
<context:component-scan base-package="com.learn"></context:component-scan>


<!--    "com.learn.mapper" 패키지 내에 있는 매퍼 인터페이스들을 스캔하고, MyBatis의 구성에 자동으로 등록하는 역할을 합니다.
        이렇게 함으로써 개발자는 매퍼 인터페이스를 별도로 등록하지 않아도 MyBatis가 자동으로 인식하고 활용할 수 있습니다. -->
    <mybatis:scan base-package="com.learn.mapper"/>

<!--    InternalResourceViewResolver 클래스의 인스턴스를 생성하고 빈으로 등록을 한다.
        prefix = /WEB-INF/views/, suffix = .jsp 라는 값을 설정했다.
        즉 /WEB-INF/views/ 디렉토리 아래에 있는 .jsp 파일을 뷰로 사용하겠다. -->
    <beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <beans:property name="prefix" value="/WEB-INF/views/" />
        <beans:property name="suffix" value=".jsp" />
    </beans:bean>

<!--     "/resources/"로 시작하는 모든 요청을 "/resources/" 디렉토리에 위치한 정적 리소스 파일로 매핑하도록 설정하는 것 -->
    <resources mapping="/resources/**" location="/resources/" />



</beans:beans>